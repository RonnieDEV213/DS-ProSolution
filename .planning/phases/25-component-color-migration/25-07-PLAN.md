---
phase: 25-component-color-migration
plan: 07
type: execute
wave: 2
depends_on: ["25-01", "25-02", "25-03", "25-04", "25-05", "25-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 4 themes (Midnight, Dawn, Slate, Carbon) display correctly across every dashboard"
    - "No hardcoded gray-* classes remain anywhere in the application except auth pages"
    - "Monospace data formatting is consistent and readable in all themes"
    - "Text hierarchy (foreground, muted-foreground) provides clear visual distinction in all themes"
    - "Interactive states (hover, focus) are visually distinct in all themes"
    - "Auth pages maintain fixed dark appearance regardless of selected theme"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete color migration across all 4 themes and all dashboards. This is the final verification checkpoint for Phase 25.

Purpose: Ensure visual parity and readability across Midnight, Dawn, Slate, and Carbon themes after migrating ~60 files from hardcoded grays to semantic tokens. Catch any missed gray classes, broken styling, or unreadable text.
Output: Verification results confirming or identifying issues across all themes.
</objective>

<execution_context>
@C:\Users\User\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\User\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-component-color-migration/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated gray-* class audit across entire codebase</name>
  <files></files>
  <action>
    Run comprehensive grep to find any remaining hardcoded gray-* classes in the entire frontend codebase:

    ```bash
    grep -rn "gray-" apps/web/src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v "login" | grep -v "login-form"
    ```

    For any results found:
    1. Check if they are in auth pages (login, signup) → expected, leave alone
    2. Check if they are in theme preview/metadata → expected if hardcoded hex, leave alone
    3. Any other results → these are missed migrations and should be fixed immediately

    Also verify monospace formatting consistency:
    ```bash
    grep -rn "font-mono" apps/web/src/components/ --include="*.tsx" | wc -l
    ```
    This should show a significant number of matches (30+) across component files.

    And verify no mixed paradigms (gray-* and semantic in same component):
    ```bash
    for f in $(grep -rl "gray-" apps/web/src/ --include="*.tsx" | grep -v login | grep -v auth); do
      echo "$f: $(grep -c 'gray-' $f) grays, $(grep -c 'text-foreground\|text-muted-foreground\|bg-muted\|bg-card\|border-border' $f) semantics"
    done
    ```
    Any file showing both grays AND semantic tokens has a mixed paradigm issue that must be fixed.

    Fix any remaining issues found. Then verify the build succeeds:
    ```bash
    npx tsc --noEmit
    ```
  </action>
  <verify>
    `grep -rn "gray-" apps/web/src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v login | grep -v login-form | grep -v auth` returns zero results (or only justified exceptions in themes.ts preview values).
    `npx tsc --noEmit` passes.
  </verify>
  <done>Zero unexpected gray-* classes remain. Build passes. No mixed paradigm files exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete color migration of ~60 component files from hardcoded gray-* Tailwind classes to semantic design tokens. Applied monospace data formatting (font-mono + background pills) to all data values across the application. All 4 theme presets should now display consistently.
  </what-built>
  <how-to-verify>
    1. Start dev server: `cd apps/web && npm run dev`
    2. Open http://localhost:3000 and log in

    **For each of the 4 themes (Midnight, Dawn, Slate, Carbon) — switch via profile settings → Theme tab:**

    3. **Admin Dashboard** (http://localhost:3000/admin):
       - Check dashboard home page colors adapt to theme
       - Check all cards, text, and backgrounds look correct
       - Verify summary numbers use monospace font

    4. **Order Tracking** (http://localhost:3000/admin/order-tracking):
       - Check records table: column headers, row text, borders
       - Verify order IDs and monetary values have monospace with subtle background pills
       - Hover over table rows — should show distinct hover state
       - Check skeleton loading state during data fetch

    5. **Users/Accounts/Invites** pages:
       - Check tables display correctly
       - Verify email addresses use monospace
       - Check dialog forms (edit user, create invite) — labels and inputs should be properly themed

    6. **Automation** (http://localhost:3000/admin/automation):
       - Check agents table, jobs table, pairing requests table
       - Verify agent IDs, job IDs display in monospace
       - Check status badges (online, offline, pending, failed) harmonize with theme

    7. **Collection** pages (eBay Sellers, Workers, History):
       - Check seller grid cards adapt to theme
       - Verify worker status cards with status indicators
       - Check progress bars use theme primary color
       - Verify metrics values display in monospace

    8. **Profile Settings** (click profile in sidebar):
       - Check all tabs render correctly in current theme
       - Verify access code displays in prominent monospace
       - Confirm theme picker preview cards still show correct preview colors

    9. **Login page** (log out or open incognito):
       - Verify login page remains fixed dark appearance regardless of theme
       - Should NOT follow the selected theme

    10. **Error pages** (visit non-existent URL):
        - Verify 404 page adapts to current theme

    **Key things to look for:**
    - No "washed out" or invisible text (contrast issues)
    - No sudden white or dark patches that don't match the theme
    - Monospace pills are subtle but readable — not too faint, not too vivid
    - Hover states are visible (buttons, table rows change on hover)
    - Status badges are distinguishable (green vs red vs yellow)
    - Login page stays dark regardless of theme
  </how-to-verify>
  <resume-signal>Type "approved" if all themes look correct, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
1. Automated audit confirms zero unexpected gray-* classes
2. Human verification confirms visual quality across all 4 themes
3. All interactive states (hover, focus, active) are visually distinct
4. Monospace data formatting is readable and consistent
5. Auth pages maintain fixed dark appearance
</verification>

<success_criteria>
- Zero hardcoded gray-* classes outside auth pages
- Visual parity confirmed in all 4 themes by human review
- Monospace data values readable in all themes
- No contrast or readability issues
- Login page stays fixed dark
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/25-component-color-migration/25-07-SUMMARY.md`
</output>
